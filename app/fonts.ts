import { Inter, Cinzel } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'], variable: '--font-sans' });
export const cinzel = Cinzel({ subsets: ['latin'], variable: '--font-serif' });
